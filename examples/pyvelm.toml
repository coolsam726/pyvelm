# Demo app module roots (used by `pyvelm db migrate --all` / `pyvelm db nuke` from repo root).
# The Vercel demo also loads `examples/modules_demo` via PYVELM_MODULE_ROOTS in vercel.json.
modules_root = "examples/modules"
