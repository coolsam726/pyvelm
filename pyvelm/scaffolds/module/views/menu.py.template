"""Sidebar entries for the {{name}} module."""

from pyvelm.builders import Menus

m = Menus("{{name}}")

MENUS = [
    m.group("home", "{{name}}", sequence=60),
    m.item("entries", "Entries", parent="home", view="{{name}}.list", sequence=10),
]
