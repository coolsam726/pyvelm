[project]
name = "{{name}}"
version = "0.1.0"
description = "A pyvelm-based application."
requires-python = ">=3.10"
dependencies = [
    "pyvelm>=0.2",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["app*"]
