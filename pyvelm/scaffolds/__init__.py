"""Bundled scaffold templates.

This package is materialised on disk by ``pyvelm/scaffolder.py``;
it is not a runtime importable surface. The ``__init__.py`` only
exists so ``importlib.resources.files("pyvelm.scaffolds")`` finds
the directory reliably across packaging configurations.
"""
