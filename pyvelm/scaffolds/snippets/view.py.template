"""Views for ``{{model}}``."""

from pyvelm.builders import field, form_view, list_view, section
from pyvelm.types import View

VIEWS: list[View] = [
    list_view(
        "{{stem}}.list",
        "{{model}}",
        title="{{title}}",
        fields=[
{{list_fields}}
        ],
        form_view="{{stem}}.form",
    ),
    form_view(
        "{{stem}}.form",
        "{{model}}",
        sections=[
{{form_sections}}
        ],
    ),
]
